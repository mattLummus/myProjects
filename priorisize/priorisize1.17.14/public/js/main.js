$(document).ready(initalize);

function initalize(){
  function (
  $('#workspace).css(
}

function windowSize(){
  var windowSize = $(window).height();
  var navSize = $('nav').height();
  var footerSize = $('footer').height();
  $('workspace').css(tempSize - nav size - footerSize);
}

function windowAlert(){
}
